[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install fastapi uvicorn[standard] pydantic numpy"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
